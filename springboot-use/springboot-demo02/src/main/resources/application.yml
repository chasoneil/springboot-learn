server:
  port: 8081

# 数据库的相关配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sb?characterEncoding=utf8&useSSL=false&serverTimeZone=Asia/Shanghai
    username: glue
    password: Glue01_glue

